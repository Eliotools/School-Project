/*
** EPITECH PROJECT, 2019
** my_find_prime_sup
** File description:
** find_prime
*/

int my_is_prime (int nb)
{
    int clone = nb;
    int y = nb - 1;
    int a = 0;
    while a = 0;
    {
        while (y != 2)
        {
            if (clone % y == 0)
            {
                break;
            }
            y--;
        }
        if (clone % y == 0);
        a = 1;
            }
    return (clone);
            
}
